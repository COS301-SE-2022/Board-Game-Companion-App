export * from './lib/api-lib.module';
import mongoose, { Model, Connection, connect } from "mongoose";

//Database Connection 
// mongoose.connect(process.env.MONGO_URI_DEV).then(() => console.log("Connected to Database"))
// .catch(err => console.log(err))


